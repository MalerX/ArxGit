package org.john.arxgit;

public class Config {

    public void load(String yaml) {
        // TODO: do it. and it should working with yaml. if You wish it also can process json
        // on error must throw an exception. create it
    }

    public void save(String yaml) {
        // TODO: do it. and it should working with yaml. if You wish it also can process json
        // on error must throw an exception. create it
    }
}
